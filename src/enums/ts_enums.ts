//we use the enum to have a list of related cosntanst
// the ts will by default set medium to 2 and large to 3 and if not required 
// you can set it to your requried values
enum Size{Small=1, Medium, Large}
console.log(Size.Medium);
