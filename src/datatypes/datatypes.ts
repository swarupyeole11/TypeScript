// you do not need to assign the number to typescript it gets assigned automatically
let a : number = 4595_594_456; //if it is a big number in typescript you can use the _ for better readability 
let b = "hello";
let c = true; 

// If you do not specify it gets specified to the type any
let d;
d=5;
