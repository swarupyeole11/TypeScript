function add(number:number):number
{
    return number;
}
//implicit passing gives error unless you specify it with type any also if it is large code base which you are mighrating in ts from js just edit the tsconfig.json file 
